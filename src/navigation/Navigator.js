import { StyleSheet, Text, View } from 'react-native'

const Navigator = () => {
  return (
    <View>
      <Text>Navigator</Text>
    </View>
  )
}

export default Navigator

const styles = StyleSheet.create({})