import { StyleSheet, Text, View } from 'react-native'

const CartStack = () => {
  return (
    <View>
      <Text>CartStack</Text>
    </View>
  )
}

export default CartStack

const styles = StyleSheet.create({})